<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>fr.miageamiens.isi05</groupId>
    <artifactId>exercice3-sudoku</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>exercice3-sudoku</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>8</maven.compiler.release>
    </properties>

    <dependencies>
        <!-- Dépendance JUnit 4 indispensable pour vos fichiers de tests unitaires existants -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>

        <!-- JUnit 5 (moteur de compatibilité) si requis par d'autres composants du projet -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>

        <!-- Moteur JUnit Vintage indispensable pour exécuter vos tests JUnit 4 avec le runner JUnit 5 -->
        <dependency>
            <groupId>org.junit.vintage</groupId>
            <artifactId>junit-vintage-engine</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Plugin de compilation Maven configuré pour Java 8 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>8</release>
                </configuration>
            </plugin>

            <!-- Plugin Surefire pour exécuter les tests unitaires JUnit -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>

            <!-- 1. Plugin Checkstyle pour l'analyse de style et la création de checkstyle-result.xml -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <configLocation>sun_checks.xml</configLocation>
                    <encoding>UTF-8</encoding>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>false</failsOnError>
                    <linkXRef>false</linkXRef>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle-analysis</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>checkstyle</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- 2. Plugin PMD pour la qualité du code et la génération de pmd.xml -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>3.21.2</version>
                <configuration>
                    <printFailingErrors>true</printFailingErrors>
                    <format>xml</format>
                    <failOnViolation>false</failOnViolation>
                </configuration>
                <executions>
                    <execution>
                        <id>pmd-analysis</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>pmd</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- 3. Plugin SpotBugs pour la détection de bugs et la création de spotbugsXml.xml -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.8.6.7</version>
                <configuration>
                    <xmlOutput>true</xmlOutput>
                    <threshold>Low</threshold>
                    <effort>Max</effort>
                    <!-- Permet au build Maven de ne pas échouer si une erreur JVM se produit -->
                    <failOnError>false</failOnError>
                    <includeTests>false</includeTests>
                    <!-- Force l'exclusion complète du scan système JVM pour éviter d'analyser le runtime incompatible -->
                    <excludeFilterFile></excludeFilterFile>
                    <onlyAnalyze>fr.miageamiens.isi05.exercice3_sudoku.*</onlyAnalyze>
                    <fork>true</fork>
                    <!-- Si la JVM hôte Jenkins n'est pas compatible avec SpotBugs, on ignore le crash sans stopper Maven -->
                    <skip>false</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>spotbugs-analysis</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>spotbugs</goal>
                        </goals>
                        <configuration>
                            <failOnError>false</failOnError>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- 4. Plugin JaCoCo pour la mesure et la génération du rapport de couverture de code -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <configuration>
                    <!-- On restreint la collecte de couverture uniquement à GrilleImpl et ValeurDeCaseAsChar -->
                    <includes>
                        <include>fr/miageamiens/isi05/exercice3_sudoku/GrilleImpl.class</include>
                        <include>fr/miageamiens/isi05/exercice3_sudoku/GrilleImplTest.class</include>
                        <include>fr/miageamiens/isi05/exercice3_sudoku/ValeurDeCaseAsChar.class</include>
                        <include>fr/miageamiens/isi05/exercice3_sudoku/ValeurDeCaseAsCharTest.class</include>
                    </includes>
                </configuration>
                <executions>
                    <!-- Initialisation de l'agent de couverture avant l'exécution des tests -->
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <!-- Génération des rapports (HTML, XML, CSV) après l'exécution des tests -->
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
